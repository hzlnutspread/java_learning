package Generics;

public class bounded_types {

    public static void main(String[] args) {

        //Bounded types =   You can create the objects of a generic class to have data of a specified derived types ex. Number
        //So in the MyGenericClass file, you would say this:
        // public class MyGenericClass <T extends Number, V extends Number>
        // This would mean it would only allow those subsets of Number to be used as inputs for the generic objects
    }
}
